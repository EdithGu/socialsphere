# No frontend env vars needed anymore for AI image generation — the
# OpenAI key now lives on the backend only (see backend/.env.example).
# This file is kept as a placeholder in case future frontend-only config
# is needed.
